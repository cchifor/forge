# Worker configuration — copy to .env and adjust. All variables are optional;
# the defaults in src/worker/config.py apply when unset.
WORKER_LOG_LEVEL=INFO
WORKER_POLL_INTERVAL_SECONDS=5.0
# Stop after N iterations (0 = run forever):
WORKER_MAX_ITERATIONS=0

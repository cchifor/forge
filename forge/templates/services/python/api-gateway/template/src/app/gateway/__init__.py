"""API-gateway support package.

Holds the downstream-service registry (:mod:`app.gateway.downstreams`) and the
service-to-service token client (:mod:`app.gateway.s2s_client`) used by the
``/api/v1/gateway`` proxy router.
"""

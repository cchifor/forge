name: forge_canvas_core
description: Framework-agnostic AG-UI reducer, SSE client, and MCP approval/iframe-bridge surface for forge canvas packages. Pure Dart — no Flutter.
version: 1.0.0-alpha.1
repository: https://github.com/cchifor/forge

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dio: ^5.7.0
  http: ^1.2.0
  json_patch: ^3.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

"""Scaffold templates for ``forge plugins scaffold-*`` commands.

Each subdirectory under this package is a Jinja2 template tree rendered
by the matching scaffold command. Files use the ``.jinja`` suffix when
they contain placeholders (the suffix is stripped on render); files
without the suffix are copied verbatim.
"""

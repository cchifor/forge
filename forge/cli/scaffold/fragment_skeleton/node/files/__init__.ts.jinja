// TODO: implement {{ name }}_impl for the node backend.
//
// Files in this tree are copied verbatim into the generated backend.
// This file lands at <backend>/__init__.ts — adjust the path under
// node/files/ to land where you actually need it (e.g.
// node/files/src/{{ name }}.ts → <backend>/src/{{ name }}.ts).

// TODO: implement {{ name }}_impl for the rust backend.
//
// Files in this tree are copied verbatim into the generated backend.
// This file lands at <backend>/lib.rs — adjust the path under
// rust/files/ to land where you actually need it (e.g.
// rust/files/src/{{ name }}.rs → <backend>/src/{{ name }}.rs).

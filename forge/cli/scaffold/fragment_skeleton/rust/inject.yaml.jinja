# inject.yaml — rust backend injection manifest for the {{ name }} fragment.
#
# Each entry pins a snippet to an anchor marker in a target file. See
# forge/fragments/_spec.py for the full schema. Delete this stub and
# replace with your real entries, then run `forge --plan --dry-run`
# to validate.
#
# TODO: replace this stub with real entries, or delete if the fragment
# is pure-files (no injection).
[]

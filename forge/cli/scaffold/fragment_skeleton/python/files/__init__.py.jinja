# TODO: implement {{ name }}_impl for the python backend.
#
# Files in this tree are copied verbatim into the generated backend.
# This file lands at ``<backend>/__init__.py`` — adjust the path under
# ``python/files/`` to land where you actually need it (e.g.
# ``python/files/src/app/{{ name }}.py`` → ``<backend>/src/app/{{ name }}.py``).

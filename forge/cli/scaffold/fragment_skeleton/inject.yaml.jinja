# Per-backend inject.yaml lives at <fragment>/<backend>/inject.yaml. This
# top-level file is a placeholder so the scaffold lays the directory
# structure visibly — delete it once you have at least one real
# <backend>/inject.yaml authored.
#
# TODO: remove this file once a real per-backend inject.yaml exists.
[]

[feature]
name = "events"
version = "1.0.0"
summary = "CloudEvents bus and transactional outbox (vendored, self-contained)."
category = "async_work"

[feature.depends]

[feature.provides]
options = ["events.bus", "events.outbox"]
fragments = ["events_core", "events_outbox"]

[feature]
name = "airlock"
version = "1.0.0"
summary = "Vendored async client for the Airlock sandbox orchestrator."
category = "platform"

[feature.depends]

[feature.provides]
options = ["airlock.client"]
fragments = ["airlock_client"]

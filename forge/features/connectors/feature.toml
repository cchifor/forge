[feature]
name = "connectors"
version = "1.0.0"
summary = "Pluggable read/write adapters (vendored, self-contained)."
category = "knowledge"

[feature.depends]

[feature.provides]
options = ["connectors.enabled", "connectors.backends"]
fragments = ["connectors_registry"]

[feature]
name = "security"
version = "1.0.0"
summary = "CSP nginx config + SBOM workflow."
category = "platform"

[feature.depends]

[feature.provides]
options = ["security.csp", "security.sbom"]
fragments = ["security_csp", "security_sbom"]

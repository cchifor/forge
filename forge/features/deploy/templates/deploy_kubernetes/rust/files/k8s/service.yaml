apiVersion: v1
kind: Service
metadata:
  name: app-rust
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/component: rust
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app
    app.kubernetes.io/component: rust
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

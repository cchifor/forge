[feature]
name = "deploy"
version = "2.0.0"
summary = "Topology-aware Helm chart deployment — per-backend Deployment/Service/HPA, frontend, Ingress, kept current on update."
category = "platform"
stability = "beta"

[feature.depends]

[feature.provides]
options = [
    "deploy.target",
]
fragments = [
    "deploy_helm_chart",
]

# Secrets template — copy to secrets.production.yaml, fill values, then encrypt:
#   sops --encrypt --in-place config/secrets.production.yaml

oidc:
  client_secret: "REPLACE-WITH-GATEKEEPER-CLIENT-SECRET"

redis:
  url: "redis://:REPLACE-WITH-REDIS-PASSWORD@redis-host:6379/0"

- target: src/lib.rs
  marker: FORGE:LIB_MOD_REGISTRATION
  position: after
  zone: generated
  snippet: |-
    // Cache adapter: Redis — concrete impl plugged in behind the
    // ``crate::ports::cache::CachePort`` trait.
    pub mod adapters;

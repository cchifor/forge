- target: src/lib.rs
  marker: FORGE:LIB_MOD_REGISTRATION
  position: after
  zone: generated
  snippet: |-
    // Cache adapter: in-process LRU — concrete impl plugged in behind
    // the ``crate::ports::cache::CachePort`` trait.
    pub mod adapters;

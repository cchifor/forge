- target: src/app/core/container.py
  marker: FORGE:APP_POST_CONFIGURE
  position: after
  zone: generated
  snippet: |-
    # Cache adapter: in-process LRU — wired behind the ``CachePort`` interface.
    from app.adapters.cache.memory import MemoryCacheAdapter

    _memory_cache_adapter: "CachePort" = MemoryCacheAdapter()

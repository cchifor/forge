- target: src/app/core/container.py
  marker: FORGE:APP_POST_CONFIGURE
  position: after
  zone: generated
  snippet: |-
    # Cache port — adapter-less startup keeps ``CachePort`` available for
    # typing only; an adapter (reliability.cache=memory|redis) wires the
    # concrete impl.
    from app.ports.cache import CachePort  # noqa: F401

[feature]
name = "object_store"
version = "1.0.0"
summary = "Blob storage port + adapters — S3/MinIO/R2 and local filesystem."
category = "platform"

[feature.depends]

[feature.provides]
options = [
    "object_store.backend",
]
fragments = [
    "object_store_port",
    "object_store_s3",
    "object_store_local",
]

# {{ project_name }}

{{ description }}

A minimal Vite + TypeScript SPA scaffolded by forge's reference Vite frontend
plugin — a worked example of registering a new frontend framework through the
plugin SDK (`forge.api.ForgeAPI.add_frontend`).

```bash
npm install
npm run dev      # serves on http://localhost:{{ server_port }} (proxies {{ api_base_url }} → backend)
npm run build    # type-checks + bundles to dist/
npm run preview  # preview the production build
```

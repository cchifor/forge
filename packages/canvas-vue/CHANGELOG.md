# Changelog

## 1.0.0-alpha.1 — unreleased

- Initial scaffold. `createCanvasRegistry` + `CanvasComponent` types.
  Real component exports (`CodeViewer`, `DataTable`, etc.) land in
  `1.0.0-alpha.4` once they're extracted from the Vue template.
